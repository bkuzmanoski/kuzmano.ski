import { SITE_NAME } from "./site.ts";

export const CONTACT_ROUTE = "/contact";
export const CONTACT_DOCUMENT_TITLE = "Contact";
export const CONTACT_DOCUMENT_DESCRIPTION = "";
export const CONTACT_DISPLAY_NAME = SITE_NAME; // Shown in the compose window's `To:` field until the contact email address loads.
